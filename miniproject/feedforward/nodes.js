class nodes{
  //this.neurons = {};
  //this.weights =
  constructor(numNeurons, neurons, weights){
    this.numNeurons = numNeurons;
    this.neurons.push = neurons;
    this.weight = weights;
  }

  draw(){
    ellipse(50, 280, 13, 13);
    for(let i = 0; i < this.numNeurons; i++){

    }
  }

  update(){

  }

}
